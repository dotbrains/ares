# Linode/Akamai

Before applying `ares` on a Linode instance:

- confirm Lish or cloud console access is available
- confirm cloud firewall rules allow the detected SSH port
- create an image or backup when the host is not disposable
- keep the original SSH session open until verification passes
