# OVH

Before applying `ares` on an OVH VPS:

- confirm rescue mode access is available
- confirm provider firewall rules allow the detected SSH port
- create a snapshot or backup when practical
- keep SSH and firewall recovery instructions nearby during apply
