# Hetzner

Before applying `ares` on a Hetzner VPS:

- confirm Console or Rescue System access is available
- confirm Hetzner firewall rules allow the detected SSH port
- create a snapshot when the host is not disposable
- keep out-of-band access available until SSH reload and firewall verification pass
