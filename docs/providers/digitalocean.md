# DigitalOcean

Before applying `ares` on a Droplet:

- confirm Droplet Console access works
- confirm cloud firewalls allow the detected SSH port
- create a snapshot when the host is not disposable
- keep the recovery console open during SSH and firewall changes
