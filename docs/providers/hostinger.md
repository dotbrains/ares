# Hostinger

Before applying `ares` on a Hostinger VPS:

- confirm browser console or recovery access is available
- confirm provider firewall rules allow the detected SSH port
- create a backup or snapshot when practical
- verify SSH remains reachable before closing the original session
