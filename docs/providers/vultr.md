# Vultr

Before applying `ares` on a Vultr instance:

- confirm web console access works
- confirm Vultr firewall rules allow the detected SSH port
- create a snapshot when the host is not disposable
- verify SSH remains reachable before closing the original session
