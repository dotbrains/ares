# AWS Lightsail

Before applying `ares` on a Lightsail instance:

- confirm browser-based SSH or serial console recovery is available
- confirm Lightsail networking allows the detected SSH port
- create a snapshot when the instance is not disposable
- verify provider firewall and host firewall rules agree before ending the session
