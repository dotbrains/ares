repos:
  - repo: local
    hooks:
      - id: ares-ci
        name: ares local CI
        entry: flox activate -c "make ci"
        language: system
        pass_filenames: false
        stages: [pre-commit]
